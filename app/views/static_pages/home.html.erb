<h1>Featured Products:</h1>

<% @showcase.each do |item| %>
  <h2 class="featured-item"><%= link_to "#{item.name}", product_path(item.id) %></h2>
  <%= item.description %>
<% end %>

<!-- <%= link_to "Create Category Test Link", new_category_path, class: "button" %> -->
